import _ from "lodash"

const title = "About accounts involved with entrypoint tables"
const textMd = _.trim(`
Accounts involved as tables
`)

const contents = {
    title,
    textMd
}

export default contents